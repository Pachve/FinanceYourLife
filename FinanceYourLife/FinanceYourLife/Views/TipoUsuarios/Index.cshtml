@model IEnumerable<FinanceYourLife.Models.TipoUsuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoPersona)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoPersona)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDTipoUsuario }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDTipoUsuario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDTipoUsuario })
        </td>
    </tr>
}

</table>
